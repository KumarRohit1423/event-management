import { robotoSerif } from "@/app/ui/fonts";
export default function TitleText() {
	return (
		<div
			className={`${robotoSerif.className} text-2xl md:text-2xl antialiased`}
		>
			Organizzer
		</div>
	);
}
