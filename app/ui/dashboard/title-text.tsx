import { roboto } from "@/app/ui/fonts";
export default function TitleText() {
	return (
		<div
			className={`${roboto.className} text-2xl font-semibold md:text-2xl antialiased`}
		>
			Organizzer
		</div>
	);
}
